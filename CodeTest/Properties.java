package CodeTest;

// import java.sql.Connection;
// import java.sql.PreparedStatement;
// import java.sql.ResultSet;
// import java.util.HashMap;
// import java.util.Map;

/**
 * 
 * Just guess, what this class for and how does it work?
 *  1) what this class(file) for?
 *      Storing Global variables used entire project. 
 *  2) how does it work?
 *      Maybe it would be loaded when the program get starting.
 * So the other class(or Object class) can access to these values.
 * Required some of information how jsp, servlet works.
 * Almost hardcoding like he said.
 * 
 */

public class Properties {
    // NSIC sample url no..
    /**
     * 
     */


    /*
    
    public static void Connect(String no){
        // what is the 'p' meaning head of names?
        
        PreparedStatement ppstmt = null;
        ResultSet prs = null;

        try{
            // DataSource -- servlet connection poll ?
            // pconn = dataSource.getConnection();
            
            
        }
    }
    */
}
